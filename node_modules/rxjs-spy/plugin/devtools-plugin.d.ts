import { BasePlugin } from "./plugin";
import { Spy } from "../spy-interface";
import { SubscriberRef, SubscriptionRef } from "../subscription-ref";
export declare class DevToolsPlugin extends BasePlugin {
    private batchQueue_;
    private batchTimeoutId_;
    private connection_;
    private posts_;
    private plugins_;
    private responses_;
    private snapshotHinted_;
    private spy_;
    private subscription_;
    constructor(spy: Spy);
    afterSubscribe(ref: SubscriptionRef): void;
    afterUnsubscribe(ref: SubscriptionRef): void;
    beforeComplete(ref: SubscriptionRef): void;
    beforeError(ref: SubscriptionRef, error: any): void;
    beforeNext(ref: SubscriptionRef, value: any): void;
    beforeSubscribe(ref: SubscriberRef): void;
    beforeUnsubscribe(ref: SubscriptionRef): void;
    teardown(): void;
    private batchDeckStats_(stats);
    private batchMessage_(message);
    private batchNotification_(notificationRef);
    private recordPlugin_(spyId, pluginId, plugin);
    private teardownPlugin_(pluginId);
    private toNotification_(notificationRef);
}
